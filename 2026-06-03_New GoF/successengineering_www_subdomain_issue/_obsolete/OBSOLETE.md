# Obsolete Files

The original issue file documented the problem on `.com` (which was incorrect).
This directory archives those older versions for reference.

The **active, correct version** is in the parent directory:
- `www_subdomain_configuration_issue.md` — CURRENT (uses .works domain)

The problem is actually on `successengineering.works` (not .com).

See parent directory `README.md` for current project structure.
